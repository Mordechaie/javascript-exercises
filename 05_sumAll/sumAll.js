const sumAll = function(num1, num2) {
    // sum variable
    let sum = 0;

    // for loop to loop through num1 to num2
    for (let i = num1; i <= num2; i ++){
        sum += i
    }
    return counter.sum()
};

// Do not edit below this line
module.exports = sumAll;
